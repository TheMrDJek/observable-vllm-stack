.env
# Одно ".crt" не матчит ничего: gitignore требует глоб. Сертификат локального
# CA публичен, но это артефакт конкретного стенда и в репозиторий не попадает.
# Приватные ключи закрыты правилом secrets/ ниже.
*.crt
*.key
data/
secrets/
*.log
.idea/
examples/.idea/
.claude
