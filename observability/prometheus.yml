global:
  scrape_interval: 15s
  evaluation_interval: 15s

# Application targets are scraped by Alloy and written here via remote_write.
# That keeps deployment/site/instance labels consistent in local and remote modes.
scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: [prometheus:9090]
